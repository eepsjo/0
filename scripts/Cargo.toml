[package]
name = "the_name_cannot_start_with_a_digit"
version = "0.0.1"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", default-features = false, features = ["blocking", "rustls-tls"] }
serde_yaml = "0.9"
chrono = "0.4"
chrono-tz = "0.8"